spring:
  jpa:
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3310/withme?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: withus
    password: withuspassword